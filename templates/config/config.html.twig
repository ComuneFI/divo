{% extends "Default/index.html.twig" %}

{% block subcontent %}


<div class="col-lg-10 mx-auto">
<use xlink:href="https://italia.github.io/bootstrap-italia/dist/svg/sprite.svg"></use>
<div class="it-timeline-wrapper">
  <div class="row">
    <div class="col-12">
      <div class="timeline-element">
        <div class="it-pin-wrapper it-evidence">
          <div class="pin-icon">
            <svg class="icon">
              <use xlink:href="#it-user"></use>
            </svg>
          </div>
          <div class="pin-text"><span>Candidati Principali</span></div>
        </div>
        <div class="card-wrapper">
          <div class="card">
            <div class="card-body">
              <p class="card-text">
              Gestisci la relazione fra il codice del sistema target (i.e. Regione Toscana) ed il sistema di origine che può gestire i Candidati
              principali della Circoscrizione con codice diverso rispetto al sistema target.
              </p>
               <a href="{{ path('configCandidati') }}">  
                <button class="btn btn-primary checkEnabled disabled"       
                 data-nextstate='COF_CANDIDATES' data-entityref='Eventi' aria-disabled="true">
                <span class="text">Configura</span>
                <svg class="icon">
                  <use xlink:href="#it-tool"></use>
                </svg>
                </button>
              </a>
            </div>
          </div>
        </div>
      </div>
    </div>
    <div class="col-12">
      <div class="timeline-element">
        <div class="it-pin-wrapper it-evidence">
          <div class="pin-icon">
            <svg class="icon">
              <use xlink:href="#it-list"></use>
            </svg>
          </div>
          <div class="pin-text"><span>Liste di preferenza</span></div>
        </div>
        <div class="card-wrapper">
          <div class="card">
            <div class="card-body">
              <p class="card-text">
            Gestisci la relazione fra il codice del sistema target (i.e. Regione Toscana) ed il sistema di origine che può gestire le Liste di Preferenza
              con codice diverso rispetto al sistema target.
              </p>
                <a  href="{{ path('configListe') }}">
              
                <button class="btn btn-primary checkEnabled disabled"       
                 data-nextstate='COF_CANDIDATES' data-entityref='Eventi' aria-disabled="true">

                <span class="text">Configura</span>
                <svg class="icon">
                  <use xlink:href="#it-tool"></use>
                </svg>
                </button>
              </a>
            </div>
          </div>
        </div>
      </div>
    </div>
    <div class="col-12">
      <div class="timeline-element">
        <div class="it-pin-wrapper it-evidence">
          <div class="pin-icon">
            <svg class="icon">
              <use xlink:href="#it-user"></use>
            </svg>
          </div>
          <div class="pin-text"><span>Candidati di preferenza</span></div>
        </div>
        <div class="card-wrapper">
          <div class="card">
            <div class="card-body">
              <p class="card-text">
              Gestisci la relazione fra il codice del sistema target (i.e. Regione Toscana) ed il sistema di origine che può gestire i Candidati
              di preferenza all'interno delle liste con codice diverso rispetto al sistema target.
              </p>
                <a href="{{ path('configCandidatiSecondari')}}">
                <button class="btn btn-primary checkEnabled disabled"    
                data-nextstate='COF_PREFERENCESCANDIDATES' data-entityref='Eventi' aria-disabled="true">
                <span class="text">Configura</span>
                <svg class="icon">
                  <use xlink:href="#it-tool"></use>
                </svg>
                </button>
              </a>
            </div>
          </div>
        </div>
      </div>
    </div>
   
</div>


{% endblock %}
